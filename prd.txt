📝 Product Requirements Document (PRD)
📌 المشروع
تصميم وتطوير صفحة هبوط ترويجية لمنتج "GlowUp Serum".

🎯 الأهداف
تعزيز الجاذبية البصرية للمنتج.

تحفيز الزوار على اتخاذ قرار الشراء.

دعم تجربة مستخدم متميزة عبر الأجهزة المختلفة.

توفير الوضع الليلي مع حفظ التفضيلات باستخدام LocalStorage.

📐 المتطلبات الفنية
1. التصميم العام:
ألوان طبيعية وناعمة:

رئيسية: #A8D5BA (أخضر فاتح), #F5F5DC (بيج), #FFFFFF (أبيض).

للنصوص الداكنة: #333333

للوضع الليلي: #121212 خلفية، #E0E0E0 للنصوص.

خطوط:

خط عربي حديث مثل "Cairo" أو "Tajawal".

عناصر تفاعلية:

Hover transitions على الأزرار والروابط.

Animations عند دخول الأقسام (Fade-In, Slide-Up).

Gradient Buttons (أخضر إلى بيج).

Responsive Layout باستخدام Flexbox / Grid وMedia Queries.

حفظ تفضيل اللون (وضع نهاري/ليلي) في localStorage.

🧱 هيكل الصفحة
✅ Header:
شعار بسيط Placeholder.

قائمة تنقل مختصرة:

الرئيسية

من نحن

تواصل معنا

زر Call To Action: "اطلب الآن"

✨ Style:

Sticky Header مع ظل خفيف.

Animation عند تمرير الصفحة.

✅ Section 1: Hero Section
صورة Placeholder جذابة تمثل المنتج.

عنوان رئيسي:

"اشعري بالنضارة الطبيعية مع GlowUp Serum"

وصف مختصر:

مثال: "تركيبة طبيعية تمنحك إشراقة لا تقاوم."

زر CTA واضح: "جربيه الآن"

✨ Style:

Full-screen أو نصف الشاشة.

Gradient background.

Fade-in على النص.

✅ Section 2: فوائد المنتج
3 إلى 4 فوائد للمنتج مع أيقونات (Font Awesome أو SVG).

✅ يرطب البشرة بعمق.

✅ يقلل من التجاعيد.

✅ مكونات طبيعية 100%.

✅ مناسب لجميع أنواع البشرة.

✨ Style:

Grid layout.

Hover Effects على البطاقات.

Animation: Zoom-in عند الظهور.

✅ Section 3: آراء العملاء (Testimonials)
2 إلى 3 تعليقات عملاء مثل:

"بشرتي تغيرت تمامًا بعد أول أسبوع!" – سارة

"أفضل منتج استخدمته لبشرتي" – ندى

✨ Style:

بطاقات بكلام العميل.

صورة افتراضية صغيرة (Avatar).

Carousel إن أمكن.

✅ Section 4: السعر والعرض الحالي
السعر الأصلي: 250 جنيه (مشطوب).

السعر بعد الخصم: 149 جنيه فقط.

عرض محدود: "لمدة 3 أيام فقط!"

✨ Style:

Highlight خاص للسعر المخفض.

عداد زمني اختياري.

Background ملون مختلف لجذب الانتباه.

✅ Footer:
روابط تواصل اجتماعي:

Facebook / Instagram (أيقونات).

رقم واتساب مباشر.

حقوق النشر: "© 2025 جميع الحقوق محفوظة لـ GlowUp"

✨ Style:

Dark أو Light حسب الـ mode.

Icon hover effects.

Divider أنيق.

🌒 الوضع الليلي (Dark Mode)
زر تبديل الوضع في أعلى الصفحة أو الـ Footer.

يتم حفظ الاختيار باستخدام localStorage.

تغير الألوان تلقائيًا (نص، خلفيات، عناصر UI).

transition سلس للانتقال بين الوضعين.

javascript
Copy
Edit
// مثال لحفظ الوضع الليلي في LocalStorage
const toggleDarkMode = () => {
  const isDark = document.body.classList.toggle("dark-mode");
  localStorage.setItem("theme", isDark ? "dark" : "light");
};

window.onload = () => {
  const theme = localStorage.getItem("theme");
  if (theme === "dark") document.body.classList.add("dark-mode");
};
🎨 CSS المطلوب:
Animations:

Fade-in, Slide-up, Zoom-in عند Scroll (باستخدام @keyframes أو IntersectionObserver).

Hover:

على الأزرار، البطاقات، الروابط.

Shadows:

للبطاقات والأقسام البارزة.

Gradients:

خلفيات الأزرار أو الأقسام المهمة.

Responsive:

باستخدام Media Queries لكل من:

الشاشات الصغيرة (≤ 768px)

الشاشات المتوسطة (≥ 1024px)

🧪 اختبارات
التحقق من عمل الصفحة على:

موبايل (iOS, Android)

شاشات لابتوب وديسكتوب

اختبار التبديل بين Dark / Light Mode.

التحقق من حفظ واسترجاع LocalStorage.

التأكد من أن الـ CTA واضح ويعمل.

